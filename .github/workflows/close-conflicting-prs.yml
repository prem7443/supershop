name: Close PRs with Merge Conflicts

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [conflict1]  # Optional: adjust to your default branch

jobs:
  check_merge_conflicts:
    runs-on: ubuntu-latest

    steps:
      - name: Dump GitHub event type
        run: echo "Event: ${{ github.event_name }}"

      - name: Dump GitHub event payload (for debugging)
        run: echo '${{ toJson(github.event) }}'

      - name: Check if PR has merge conflicts
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = context.payload.pull_request;

            if (!pr) {
              throw new Error("❌ No pull request data in context. This step runs only on pull_request events.");
            }

            const { data: initialPR } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.number,
            });

            let attempts = 0;
            let mergeable = initialPR.mergeable;

            while (mergeable === null && attempts < 5) {
              console.log(`⏳ Attempt ${attempts + 1}: Waiting for GitHub to calculate mergeability...`);
              await new Promise(resolve => setTimeout(resolve, 3000)); // wait 3 seconds

              const { data: updatedPR } = await github.rest.pulls.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: pr.number,
              });

              mergeable = updatedPR.mergeable;
              attempts++;
            }

            if (mergeable === false) {
              console.log(`❌ PR #${pr.number} has merge conflicts. Closing...`);

              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: pr.number,
                body: "🚫 This pull request has merge conflicts and will be closed automatically. Please resolve the conflicts and open a new PR.",
              });

              await github.rest.pulls.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: pr.number,
                state: 'closed',
              });

            } else if (mergeable === true) {
              console.log(`✅ PR #${pr.number} is mergeable.`);
            } else {
              console.log(`⚠️ Could not determine mergeability for PR #${pr.number}.`);
            }

      - name: Notify push (optional)
        if: github.event_name == 'push'
        run: echo "🚀 A push occurred to branch ${{ github.ref }}"
